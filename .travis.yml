language: python
python:
    - "3.5"
    - "3.6" 
    - "3.7"
    - "3.8"

services:
    - docker

before_script: pip install docker-compose

script:
    - docker-compose run salemana sh -c "python manage.py test && flake8"